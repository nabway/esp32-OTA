#pragma once

// Actual app version. Bump this string when building a new firmware image
// (for example 1.0 -> 1.1) so the device can show it on boot.
static constexpr const char* APP_VERSION = "1.4";
